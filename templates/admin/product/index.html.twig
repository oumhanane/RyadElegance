{# {% extends 'admin/base.html.twig' %}

{% block title %}Product index{% endblock %}

{% block body %}
    <h1>Product index</h1>

    <table class="table">
        <thead>
            <tr>
                <th>Id</th>
                <th>Name</th>
                <th>Price</th>
                <th>Image</th>
                <th>actions</th>
            </tr>
        </thead>
        <tbody>
        {% for product in products %}
            <tr>
                <td>{{ product.id }}</td>
                <td>{{ product.name }}</td>
                <td>{{ product.price }}</td>
                <td>{{ product.image }}</td>
                <td>
                    <a href="{{ path('admin_product_show', {'id': product.id}) }}">show</a>
                    <a href="{{ path('admin_product_edit', {'id': product.id}) }}">edit</a>
                </td>
            </tr>
        {% else %}
            <tr>
                <td colspan="5">no records found</td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

    <a href="{{ path('admin_product_new') }}">Create new</a>
{% endblock %} #}




{% extends 'admin/base.html.twig' %} 
{% block title %}
{% endblock %}

{% block body %}
	<div class="container" py-5>  
		<div class="row">

		{% include "_messages_flash.html.twig" %}

			<div class="col-12 mt-5 d-flex align-items-center justify-content-between">
				<h3>Liste des produits</h3>
				<a class="btn btn-primary" href="{{ path('admin_product_new') }}">Ajouter</a>
			</div>

            <div class="navigation">
                {{ knp_pagination_render(products) }}
            </div>

			{% for product in products %}
				<div class="col-sm-12 pt-3 pb-3 col-md-6 col-lg-4">
					<div class="card">
						<img src="{{ product.image }}" class="card-img-top imageCard" alt="image_product">
						<div class="card-body">
							<h5 class="card-title">{{ product.name | upper }}</h5>
                            <p> {{ product.price | priceEuro }}
							<a href="{{ path('admin_product_edit', {'id' : product.id } ) }}" class="btn btn-primary w-100 mt-3">Gérer</a>
						</div>
					</div>
				</div>
			{% endfor %}

            <div class="navigation">
                {{ knp_pagination_render(products) }} 
            </div>
		</div>
	</div>

{% endblock %} 